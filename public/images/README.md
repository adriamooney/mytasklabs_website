# Images

Drop image files here (e.g. `logo.png`, `photo.jpg`).

Reference them in your app by path from the site root:

- `public/images/logo.png` → use `/images/logo.png` in `src` or `href`
- In JSX: `<img src="/images/logo.png" alt="..." />` or Next.js: `<Image src="/images/logo.png" ... />`
